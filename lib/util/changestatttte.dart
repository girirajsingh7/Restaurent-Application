import 'package:flutter/cupertino.dart';

class chaneCounter extends StatefulWidget {
  const chaneCounter({Key? key}) : super(key: key);

  @override
  State<chaneCounter> createState() => _chaneCounterState();
}

class _chaneCounterState extends State<chaneCounter> {
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
